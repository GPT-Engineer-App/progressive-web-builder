import { Box, Heading, Text } from "@chakra-ui/react";

const Account = () => {
  return (
    <Box p={4}>
      <Heading mb={4}>Account</Heading>
      <Text>Account page content goes here.</Text>
    </Box>
  );
};

export default Account;